#!/usr/bin/env node
// Preflight stub to satisfy the prepush hook. Add real checks later if needed.
// Examples you might add later: branch protections, env var presence, file linting gates.
//
// For now, exit success so "npm run prepush" doesn't block pushes.
process.exit(0);